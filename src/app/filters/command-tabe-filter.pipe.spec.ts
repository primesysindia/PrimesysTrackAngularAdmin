import { CommandTabeFilterPipe } from './command-tabe-filter.pipe';

describe('CommandTabeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommandTabeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
